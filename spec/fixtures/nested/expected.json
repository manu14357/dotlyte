{
  "description": "Nested keys: underscore-separated env var keys map to nested config objects",
  "options": {},
  "env_vars": {},
  "expected": {
    "app_name": "my-app",
    "database_host": "localhost",
    "database_port": 5432,
    "database_credentials_username": "admin",
    "database_credentials_password": "secret",
    "cache_enabled": true,
    "cache_ttl": 3600,
    "cache_backends": ["redis", "memcached"]
  }
}
